PORT=4000
## Mongo connection string
# Local (no auth):
# MONGO_URI=mongodb://localhost:27017/catering
# MongoDB Atlas (SRV) example - replace <username>, <password>, <cluster>, <dbname>:
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority&appName=<appName>

JWT_SECRET=change_this_secret

# Comma-separated list of allowed frontend origins for CORS
# Local dev origins (localhost:4200, localhost:4000) are always allowed automatically
ALLOWED_ORIGINS=https://dietflow.in,https://www.dietflow.in

# NOTES:
# - Do NOT commit real credentials to source control. Keep this file out of git or use environment-specific config management.
# - If you enable MongoDB authentication, create a DB user and use the username/password in the URI.
# - For Agenda jobs and multi-document transactions, use a replica set (Atlas clusters are already replica sets).