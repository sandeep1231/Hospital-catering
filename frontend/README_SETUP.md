Angular workspace created from existing components.

Run:
- cd d:\catering\frontend
- npm install
- npm start

If the Angular CLI is not installed globally, npm start will use the local CLI from devDependencies.
